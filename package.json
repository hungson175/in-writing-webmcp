{
  "name": "in-writing-webmcp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "vite --host 127.0.0.1 --port 4317",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4321"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "vite": "8.0.0",
    "vitest": "4.0.18"
  }
}
